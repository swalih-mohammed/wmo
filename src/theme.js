import { createTheme } from '@mui/material/styles';


const theme = createTheme({
  palette: {
    primary: {
      main: "#390099",
    },
    common: {
      black: "#2b2d42",
      whatsup: "#25d366"
    },
    secondary: {
      main: '#651fff',
    },
    text:{
      main: "#2b2d42"
    }
  },
});

export default theme;
